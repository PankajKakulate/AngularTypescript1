function DisplayPercentage(Marks:number,outOf:number):number{
    
    var per:number=0;
    per=(Marks/outOf)*100;
    return per;
}

DisplayPercentage(800,1000);
DisplayPercentage(900,1000);
//Display("Sneha",97);